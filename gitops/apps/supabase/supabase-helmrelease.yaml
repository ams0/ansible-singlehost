---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: supabase
  namespace: flux-system
  labels:
    app: supabase
spec:
  interval: 10m
  timeout: 10m
  targetNamespace: supabase
  chart:
    spec:
      chart: supabase
      sourceRef:
        kind: HelmRepository
        name: supabase
        namespace: flux-system
      interval: 5m0s
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    environment:
      auth:
        API_EXTERNAL_URL: https://supabase.vps.kubespaces.cloud
      studio:
        SUPABASE_PUBLIC_URL: https://supabase.vps.kubespaces.cloud
    ingress:
      enabled: false
    secret:
      dashboard:
        secretRef: supabase-dashboard-creds
        secretRefKey:
          username: username
          password: password
          openAiApiKey: openAiApiKey
    deployment:
      kong:
        enabled: true