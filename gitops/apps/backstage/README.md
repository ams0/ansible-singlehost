# Backstage

Spotify's open-source developer portal framework for building internal developer portals with a plugin-based architecture.

## Status: Not Deployed

This is currently a **placeholder/skeleton**. Only the namespace definition exists -- no HelmRelease, chart source, or routing is configured yet. The kustomization is nearly empty.

## Resources

| File | Purpose |
|------|---------|
| `namespaces.yaml` | Namespace reservation |
| `kustomization.yaml` | Flux kustomization (minimal) |
