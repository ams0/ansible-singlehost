apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alarik-pvc
  namespace: alarik
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
