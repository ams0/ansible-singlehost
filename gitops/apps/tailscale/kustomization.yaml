---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - tailscale-helmrepo.yaml
  - tailscale-helmrelease.yaml
  - tailscale-dnsconfig.yaml

# Removed coredns-patch.yaml — k0s manages CoreDNS ConfigMap directly.
# Manual configuration of ts.net forwarding is already applied via k0s.
