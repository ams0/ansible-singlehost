---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thanos-store
  namespace: observability
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi  # Adjust based on your retention needs
  # storageClassName: ""  # Uncomment and specify if needed
